import React from "react";
// import { useSession } from "next-auth/react";

export default function Page() {
  // const { data: session, status } = useSession();

  // if (status === "authenticated") {
  // console.log(status);

  // return <React.Fragment>Account page</React.Fragment>;
  // }
  return <React.Fragment>Account page</React.Fragment>;
}
